<!DOCTYPE html>

<title>Formulaire d'édition</title>

{% block body %}
    <h1>Edit Media</h1>

    {{ include('media/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('media_index') }}">Retour vers la liste</a>

    {{ include('media/_delete_form.html.twig') }}
{% endblock %}
