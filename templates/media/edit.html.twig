<!DOCTYPE html>

<title>Edit Media</title>

{% block body %}
    <h1>Modfication photo</h1>

    {{ include('media/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('media_index') }}">Retour vers les photos</a>

    {{ include('media/_delete_form.html.twig') }}
{% endblock %}
