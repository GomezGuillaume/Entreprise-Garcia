{% extends 'Base/base.html.twig' %}



{% block content %}
    <h1 class="text-center pt-5">Index Photos</h1>

    {% for medium in media %}

        <div class="image-admin image">
            <figure>
                <img src="{{ asset('img/peinture') }}/{{ medium.name }}" class="img-fluid" alt="{{ medium.title }}">
                <figcaption>{{ medium.title }}</figcaption>
            </figure>
        </div>

        <a href="{{ path('media_show', {'id': medium.id}) }}">Voir la photo</a>

    {% else %}
        <tr>
            <td colspan="4">no records found</td>
        </tr>
    {% endfor %}

    <a href="{{ path('media_new') }}">Ajouter une image</a>

    <a href="{{ path('app_logout') }}">Se déconnecter</a>
{% endblock content %}
