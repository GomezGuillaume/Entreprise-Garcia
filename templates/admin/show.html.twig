{% extends 'Base/base.html.twig' %}


{% block content %}
    <h1 class="text-center">Media</h1>

    <div class="container">
        <img src="{{ asset('img/peinture') }}/{{ medium.name }}" class="img-resp" alt="{{ medium.title }}">
        <span>{{ medium.title }}</span>
    </div>

    <div class="btn"><a href="{{ path('media_index') }}">Retour vers les photos</a></div>

    <div class="btn"><a href="{{ path('media_edit', {'id': medium.id}) }}">Éditer la photo</a></div>

    {{ include('admin/_delete_form.html.twig') }}

{% endblock content %}
